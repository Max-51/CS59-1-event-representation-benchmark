# Learning-Based Classification Artifacts

Recommended layout:

```text
artifacts/classification/learning/<dataset>/<run_tag>/
```

Legacy snapshots are preserved under `legacy_results/` and `legacy_outputs/`.
