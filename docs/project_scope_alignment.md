# Project Scope Alignment

This repository tracks three executable benchmark tasks:

1. Classification
2. Optical Flow
3. Object Detection

The implementation scope is a **unified downstream benchmark**:

- same task-level protocol within each task
- same local runnable method set per task
- standardized artifact outputs for comparison

Paper-native full reproduction beyond released code remains out of scope.
