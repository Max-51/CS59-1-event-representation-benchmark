# CIFAR10-DVS 分类任务说明（2026-05-19）

## 1. 任务目标

完成 `RUN_TAG=20260519_cifar10dvs_aligned_cls` 的 CIFAR10-DVS 分类实验，输出 learning-based 与 traditional 两组方法的可对比结果汇总，并保存可直接用于组内同步的结构化文件。

## 2. 实验设置（本轮）

- 数据集：`cifar10dvs`
- 训练参数（对齐当前分类主线）：
  - `epochs=100`
  - `batch_size=32`
  - `lr=0.0001`
  - `weight_decay=0.0001`
  - `seed=42`
  - `max_events=50000`
- 早停：`patience=10`
- 设备：`cuda`

## 3. 方法覆盖

- learning-based（7）：
  - `est`
  - `ergo`
  - `event_pretraining`
  - `matrix_lstm`
  - `evrepsl`
  - `get`
  - `omnievent`
- traditional（5）：
  - `event_frame`
  - `binary_event_image`
  - `timestamp_image`
  - `time_surface`
  - `voxel_grid`

## 4. 结果文件

本次汇总文件：

- `outputs/analysis/cifar10dvs_20260519_cifar10dvs_aligned_cls_summary.csv`
- `outputs/analysis/cifar10dvs_20260519_cifar10dvs_aligned_cls_summary.md`

## 5. 本轮关键结论

- best learning: `est`，`57.45%`
- best traditional: `event_frame`，`41.95%`
- learning 与 traditional 最优差距：`15.5` 个百分点

## 6. 备注

- 本次提交仅整理并上传结果与说明文档，不改动实验结果本身。
- 若后续需要与 TEBN 切分严格对齐，应单独固定 split 协议并重新跑整套结果。
